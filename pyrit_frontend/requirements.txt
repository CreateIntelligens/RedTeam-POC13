# PyRIT Frontend Dependencies

# Web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Pydantic for data validation
pydantic>=2.5.0

# HTTP client
httpx>=0.25.0

# Core dependencies (should already be installed with PyRIT)
# pyrit-ai  # Install from source for now

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0